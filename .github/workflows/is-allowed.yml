name: test is-allowed

on:
  push:

jobs:
  test-is-allowed:
    runs-on: ubuntu-latest
    steps:
      - uses: dscho/gitgitgadget/is-allowed@v1
        with:
          login: dscho
      - uses: dscho/gitgitgadget/is-allowed@v1
        with:
          login: desktop
